{
  "name": "lux-command-center-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "Lux Auto Command Center — Google Apps Script web app (Google-native UI, CRM as invisible backend).",
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "open": "clasp open",
    "deploy": "clasp push --force && clasp deploy --description \"Lux Command Center\"",
    "logs": "clasp logs --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
